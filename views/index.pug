extends layout

block content
  h1 Ты и твои друзья
  p Жми кнопку, чтобы вспомнить своих друзей

  form(class="form-signin" method="POST" action="/auth")
    - var buttonClass = ['btn', 'btn-lg', 'btn-primary', 'btn-block'];
    button(class=buttonClass type="submit") авторизоваться
